name: pwa_info
description: "A Flutter package for detecting device and browser information in Progressive Web Apps (PWAs). Supports OS, browser, mobile, and standalone mode detection."
version: 1.2.0
homepage: https://github.com/edris-mazhari/pwa_info
repository: https://github.com/edris-mazhari/pwa_info
issue_tracker: https://github.com/edris-mazhari/pwa_info/issues
platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

environment:
  sdk: ^3.5.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  universal_html: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
